on:
  workflow_dispatch:
  push:
jobs:
  ci:
    runs-on: ubuntu-latest
    outputs: 
     output1: ${{steps.IP.outputs.ip}}
    steps:
      - name: 🛒 Checkout
        uses: actions/checkout@v2
      - name: 🔎 Check_IP
        id: IP
        run: curl https://api.ipify.org
      
      - name: Print_IP
        run: echo ${{output1}}
